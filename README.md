adhd took over and im doing this now idk.

wrote a custom unix shell in C from scratch. it handles built-ins, I/O redirection, and custom tab autocompletion (for now).

some standard unix commands felt like a chore to remember, so i changed some (`echo` is now `print` and `type` is `whatis`, `cd` is `go`).

### to run:
`make && ./shell`

probably abandoning this tomorrow.